import React from 'react';

const AcceptedTabRow = props => {
    
    return (
        <div className="container">
            {/* stuktura tog reda u tablici */}
            <span>{props}</span>
            <span>{}</span>
            <span>{}</span>
            <span>{}</span>
            <span>{}</span>
        </div>
    )

}